.authform
	= simple_form_for @playlist do |f|
		%h3 #{action} Playlist
		= f.input :name, :autofocus => true, :required => true
		= f.input :description, :required => true, :as => :text
		= f.button :submit, action+' Playlist', :class => 'btn-primary right'